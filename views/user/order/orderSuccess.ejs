<style>
    body {
      background-color: #f8f9fa;
    }
    .success-container {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding-top: 50px;
    }
    .success-card {
      background: white;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      width: 100%;
      max-width: 600px;
    }
    .success-card i {
      font-size: 60px;
      color: #28a745;
    }
    .success-card h2 {
      margin-top: 20px;
      font-weight: 700;
    }
    .success-card .details {
      text-align: left;
      margin-top: 20px;
    }
    .success-card .details p {
      margin: 5px 0;
    }
    .btn-home {
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <div class="container success-container">
    <div class="success-card">
      <i class="bi bi-check-circle-fill"></i>
      <h2>Thank You for Your Order!</h2>
      <p>Your order has been successfully placed.</p>

      <!-- Order Details -->
      <div class="details mt-4">
        <p><strong>Order ID:</strong> <%= order.orderId %></p>
        <p><strong>Event Date:</strong> <%= order.booking.eventDate %></p>
        <p><strong>Total Amount:</strong> ₹<%= order.totalAmount %></p>
        <p><strong>Payment Method:</strong> <%= order.paymentMethod %></p>
        <p><strong>Order Status:</strong> <%= order.status %></p>
      </div>

      <a href="/" class="btn btn-success btn-home">Continue Shopping</a>
      <a href="/orderDetails/<%=order._id%>" class="btn btn-outline-primary btn-home">View Your Orders</a>
    </div>
  </div>